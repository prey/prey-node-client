describe('HTTP transport', function(){
  
  /*
  
   - sends multipart data
   - sets Prey user agent
   - should not react to specific HTTP status codes
   - if connect fails, returns error object
  
  */
  
})