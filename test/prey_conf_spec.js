

describe('prey_conf_spec #wip', function(){
  it('it should be valid ini format (with # instead of ; though)');
  it('driver should be set to control-panel');
  it('host should be set to control.preyproject.com');
  it('protocol should be set to https');
  it('api_key should be empty');
  it('device_key should be empty');
});
